import 'package:flutter/material.dart';

class UploadImageComponent extends StatelessWidget {
  const UploadImageComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Material(
        color: Colors.transparent,
        child: Container(
          color: Colors.red,
          child: Text("data"),
        ),
      ),
    );
  }
}
